const TransferMultiple = artifacts.require("TransferMultiple")

module.exports = async function (deployer, network, accounts) {
  // await deployer.deploy(TransferMultiple, {from: accounts[0], value: "100000000000000000"})
}
